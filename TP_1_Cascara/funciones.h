#ifndef FUNCIONES_H_INCLUDED
#define FUNCIONES_H_INCLUDED

/** \brief suma de los numeros
 *
 * \param suma1, suma los valores a y b
 * \return suma1, devuelve el resultado de la suma
 *
 */

/** \brief resta de los valores
 *
 * \param resta1,resta los valores a y b
 * \return resta1, devuelve el resultado de la resta
 *
 */

/** \brief division de los valores
 *
 * \param div, divide los numeros que contienen a y b mientras que b no sea cero
 * \return div, retorna el resultado de la divicion
 *
 */

/** \brief multiplicacion de dos numeros
 *
 * \param mult, multiplica los valores de a y b
 * \return mult, retorna el resultado de la multiplicacion
 *
 */

/** \brief factoreo del valor de A
 *
 * \param fact, multiplica los numero anteriores y a si mismo
 * \param i, recorre los valores de cada numero
 * \return fact, retorna el factorial de del numero
 *
 */
/** \brief llamadaFuncion llama a todas las funciones anteriores
 *
 * \return retorna las soluciones de las operaciones hechas en cada funcion
 *
 */


float suma(float,float);
float resta(float,float);
float division(float,float);
float multiplicacion(float,float);
long int factoreo(long int);
float llamadaFuncion(float,float);

#endif // FUNCIONES_H_INCLUDED
